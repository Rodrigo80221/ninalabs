import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais
  static const Color background = Color(0xFFFCFBFC); // Off-white quente e sofisticado
  static const Color quartzPink = Color(0xFFFCEDEC);
  static const Color terracotta = Color(0xFFC1121F);
  static const Color terracottaHover = Color(0xFFA10F19);
  
  // Cores Header
  static const Color headerBackground = Colors.white; // Fundo branco mais clean
  
  // Cores de Texto
  static const Color textDark = Color(0xFF1D1D1D); // Cinza bem escuro, mais suave que preto
  static const Color textLight = Color(0xFF777777); // Secondary text
  
  // Cores de Status
  static const Color statusPendingBg = Color(0xFFFCEDEC);
  static const Color statusPendingText = Color(0xFFC1121F);
  
  static const Color statusPostedBg = Color(0xFFE8F3F1); 
  static const Color statusPostedText = Color(0xFF3B8A7E);

  static const Color successGreen = Color(0xFF225732); // Verde mais escuro e elegante

  static const Color errorText = Color(0xFFD64545);

  // Cards e Elementos
  static const Color surface = Colors.white; // Cards
  static const Color border = Color(0xFFE8E2E1); // Borders
}
